---
import ButtonLink from './ButtonLink.astro';
import json from '../assets/buttons.json'
---

{
// i stole this
json.map((button) => (
	<ButtonLink image={button.img} href={button.href} alt={button.alt} />
))
}
